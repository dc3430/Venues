import React from 'react';
import { create } from 'domain';
import styles from './create.module.css'

const create = (props) => (
    <div className={styles.create}> test </div>
);

export default create;